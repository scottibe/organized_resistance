<% if logged_in? %>
  <div class="new-event">
    <div class="col-md-8 col-md-offset-3">
      <h2>Create A New Event</h2>
    </div>    
  </div> 
  <%= render partial: "form", locals: { event: @event } %>
<% else %>
  <p>Must be signed in to vew this page</p>
<% end %>  

