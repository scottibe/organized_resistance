<% if logged_in? %>
    <div class="new-event">
      <div class="col-md-8 col-md-offset-3">
        <h2>Make A Statement</h2>
      </div>  
    </div>    
    <%= render partial: "form", statement: Statement.new, locals: { statement: @statement } %>
<% else %>  
  Must be signed in to view this page
<% end %>  

